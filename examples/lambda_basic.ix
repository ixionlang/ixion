use <std>

pub def main() {
    var add = lambda (a: int, b: int): int {
        return a + b
    }

    std::println(add(20, 22))

    std::println(lambda (x: int): int {
        return x * x
    }(7))
}
